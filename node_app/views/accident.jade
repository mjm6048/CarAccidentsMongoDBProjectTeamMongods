extends layout

block content
    img(src="/accident/img/" + accident.Weather_Condition, alt="Weather Image", width="100%", height="100%")
    h3= `Severity: ${accident.Severity}`
    h3= `Start_Time: ${accident.Start_Time}`
    h3= `End_Time: ${accident.End_Time}`
    h3= `Coordinates: (${accident.Start_Lat}, ${accident.Start_Lng})`
    h3= `Description: ${accident.Description}`
    h3= `Location: ${accident.Street}, ${accident.City}, ${accident.County}, ${accident.State}, ${accident.Zipcode}, ${accident.Country}`
    h3= `Weather_Timestamp: ${accident.Weather_Timestamp}`
    h3= `Weather_Condition: ${accident.Weather_Condition}`
    div= `Comments:`
       // each item in accident.Comments
           // p= item
    form(action="/comment/:id", method="POST")
        input(type="text", name="comment", placeholder="add a comment")
        button(type="submit") Comment
